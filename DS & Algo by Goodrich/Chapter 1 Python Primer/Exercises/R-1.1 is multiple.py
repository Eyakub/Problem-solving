def is_multiple(n, m):
    return n % m == 0


print(is_multiple(4, 2))

# multiple means 4 is divided by 2 or not
# /or For examples, 2, 4, 6, 8, and 10 are multiples of 2.
# To get these numbers, you multiplied 2 by 1, 2, 3, 4, and 5,
# which are integers. A multiple of a number is that number multiplied by an integer
